
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)	
	ЭлементыПараметраДанных = Отчет.КомпоновщикНастроек.ФиксированныеНастройки.ПараметрыДанных.Элементы;		
	Если Параметры.Свойство("account") Тогда
		ЭлементыПараметраДанных.Найти("account").Значение = Параметры.account;
		ЭлементыПараметраДанных.Найти("account").Использование = Истина;
	КонецЕсли;	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ЭтаФорма.СкомпоноватьРезультат();
КонецПроцедуры

